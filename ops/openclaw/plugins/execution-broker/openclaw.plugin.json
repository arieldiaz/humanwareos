{
  "id": "execution-broker",
  "name": "Humanware Execution Broker",
  "description": "Deterministic execution profile routing and canonical delivery ledger",
  "configSchema": {
    "type": "object",
    "required": ["catalogPath", "dataRoot"],
    "properties": {
      "catalogPath": {"type": "string", "minLength": 1},
      "dataRoot": {"type": "string", "minLength": 1}
    },
    "additionalProperties": false
  }
}
