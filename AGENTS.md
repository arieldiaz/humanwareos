# Codex adapter for life-os

This repo's shared operating rules live in `CLAUDE.md`. Treat that file as the source of truth for agent culture and workflow, even when running as Codex rather than Claude.

At the start of non-trivial work, read `CLAUDE.md`, then follow any relevant agent file in `agents/`.
